add_executable(maincpp
    ${SRC_SOURCES}
    main.cpp
)

target_link_libraries(maincpp
    PRIVATE
        xxx
)

target_compile_options(maincpp
    PRIVATE
        -Wall
        -Wextra
)

set_target_properties(maincpp PROPERTIES
    RUNTIME_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}"
)


add_executable(mainc
    ${SRC_SOURCES}
    main.c
)

target_link_libraries(mainc
    PRIVATE
        xxx
)

target_compile_options(mainc
    PRIVATE
        -Wall
        -Wextra
)

set_target_properties(mainc PROPERTIES
    RUNTIME_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}"
)
