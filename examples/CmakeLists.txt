set(EXAMPLE_SOURCES
)

add_executable(main
    ${EXAMPLE_SOURCES}
    main.cpp
)

target_link_libraries(main
    PRIVATE
        xxx
)

target_compile_options(main
    PRIVATE
        -Wall
        -Wextra
)
